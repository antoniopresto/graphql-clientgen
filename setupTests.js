require('isomorphic-fetch');

// const path = require('path');
// const fs = require('fs-extra');
//
// const CWD = process.cwd();
// const generatedFilesDest = path.resolve(CWD, `build/generated/${Date.now()}`);
//
// afterAll(async () => {
//   // await fs.rmdir(generatedFilesDest);
// });
